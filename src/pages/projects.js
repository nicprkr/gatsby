import React from "react"

import Layout from "../components/layout"
import SEO from "../components/seo"

const Projects = () => (
  <Layout>
    <SEO title="Projects" />
    <h1>Projects</h1>
    <p>Unfortunately I'm still learning how to work with Gatsby and the projects aren't quite ready to view!</p>
  </Layout>
)

export default Projects
