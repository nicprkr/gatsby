import React from "react"

import Layout from "../components/layout"
import SEO from "../components/seo"

const About = () => (
  <Layout>
    <SEO title="About" />
    <h1>About</h1>
    <p>Unfortunately I'm still learning how to work with Gatsby and the about aren't quite ready to view!</p>
  </Layout>
)

export default About
